/**
 * Dashboard (placeholder fondasi). Diisi penuh oleh Deputi Sonet — M3 §C.2
 * (summary cards + grafik 7 hari + top items). Pakai SWR + /api/dashboard/*.
 */
export default function DashboardPage() {
  return (
    <div className="card">
      <h1 style={{ fontSize: 'var(--text-xl)', marginBottom: 'var(--space-2)' }}>Dashboard</h1>
      <p style={{ color: 'var(--color-text-muted)' }}>
        Fondasi M3 siap. Halaman ini akan diisi konten dashboard (M3 §C.2).
      </p>
    </div>
  );
}

